# 002 — Adversarial panel, 2026-08-10 (post-deploy)

Ten independent review passes: intake, five criterion lenses, Stage-One gate,
correctness/vocabulary audit, depth/presentation audit, synthesis. Judged `main`
@ `19785be` with the live instance at https://aphrodite.max-gutowski.de and a
local dev server in fixture mode. **Zero YouCam units spent** — standing order 1
was in force and not waived; the ledger held 0/8 across every run the panel drove. Delta against review 001.

## Stage One: **FAIL**

- GATE 1 PASSES, and cleanly — Skin Analysis + Skin-Tone + Apparel VTO + Photo Lighting chained in one observable run, skin/colour read visibly driving the garment and the countdown, ~4.4 s end to end. It passes on fixtures, which is why gate 2 exists separately.
- GATE 2 FAILS (two independent legs). Leg one: the gate's own cited evidence does not exist for a judge — app/api/dev/verify/route.ts hard-returns 404 under NODE_ENV=production and I re-confirmed HTTP 404 against https://aphrodite.max-gutowski.de/api/dev/verify today. Leg two: zero YouCam packets leave the box on any path an anonymous judge can reach; lib/youcam/runtime.ts forces fixtures ON for any un-unlocked visitor regardless of YOUCAM_FIXTURES, live /healthz self-reports youcam:'key_present_unverified', degraded:true, and live_runs_used held at 0/8 across every run this panel drove. No committed receipt with a genuine Perfect Corp task_id exists anywhere in the repo.
- GATE 3 FAILS on its third leg. The hosted instance is genuinely up and the keyless demo genuinely works out of the box — a real advance over the 2026-08-09 baseline, which recorded no DNS at all. But 'a role code unlocks the live paths' is not judge-reachable: the landing page links to /unlock zero times (I grepped the live HTML: 0), no code is published in any testing field because no submission exists, and submission/devpost.md 'How to test it' still offers only git clone + npm run dev. grep for 'max-gutowski' in README.md and submission/devpost.md: 0 hits in both.
- GATE 4 FAILS. Two competing cuts exist and neither is published on any host: submission/aphrodite-demo.mp4 (87.200 s, gitignored at .gitignore:49) and /home/sansforensics/aphrodite-demo/aphrodite-demo.mp4 (122.306 s, outside the repo). hackathon/config.json records submission.videoUrl='UNKNOWN'; the only youtube/vimeo hit in the whole tree is config.json conceding the video is not uploaded. Public repo + MIT LICENSE + README chapter all pass; the video does not, and 'planned' does not pass.
- GATE 5 IS BREACHED AT THE CLAIM LAYER. The render layer is exemplary — announcement bar, per-image captions, the ledger's 'Nothing was called just now', a refine step that declines to render rather than inventing an image. But components/Concierge.tsx:210-228 renders unconditionally, and I confirmed both strings in the live HTML today: '4 / Perfect Corp APIs chained in one run — skin, color, try-on, lighting' and '0–100 / skin health scores, read from your own photo'. In demo mode nothing is called and the ten scores are one fixture identical for every photo. state.demo is already in scope. This is verbatim the over-claim the team fixed 700 lines below and congratulates itself for in the comment at Concierge.tsx:904.
- AGGRAVATING, same family, newly found this cycle: components/BeforeAfter.tsx clips the `after` image to the LEFT of the frame while pinning beforeLabel left and afterLabel right, so on the hosted hero the YouCam apparel render is labelled 'The photo' and the untouched original is labelled 'YouCam render'. The flagship proof card teaches a judge the opposite of what Perfect Corp's API did, under a caption reading 'Not an illustration, not a stock pair.'

## Official scorecard — 30/50

| Criterion | Score |
|---|---|
| technological implementation | **6**/10 |
| design | **6**/10 |
| potential impact | **5**/10 |
| quality of the idea | **7**/10 |
| special skin vto fusion | **6**/10 |
| **Total** | **30/50** |

**Submission Depth & Presentation: 3.5/10** (diagnostic, not folded into the total)

### Delta vs 001

> OFFICIAL 30/50, up +4 from 26/50 at review 001 (2026-08-09). By criterion: technological_implementation 4→6 (+2: hosted, anonymously drivable, deploy artifacts now tracked with a fail-closed health gate, the four-API chain observable in one run), design 6→6 (0: real craft gains — measured contrast tokens, 320px and 200%-zoom reflow, honest empty states — offset by two newly found defects that sit exactly on the criterion's own anchors, the inverted before/after provenance pills and a file input that is out of the tab order), potential_impact 4→5 (+1: a real cross-category priced basket and a horizon-differentiated countdown are now drivable; the date parser still cannot read a calendar date and no first-party retail signal exists), quality_of_the_idea 6→7 (+1: the skin-score-as-scheduling-signal wedge holds up on inspection; the LLM half of it still never runs for a judge), special_skin_vto_fusion 6→6 (0: same fixture invariance, plus two whole run classes where the chain collapses). Submission Depth & Presentation 3→3.5 (diagnostic, not folded in). Stage One: FAIL both times — gates 2, 3, 4 then; gates 2, 3, 4 plus a gate-5 claim-layer breach now. The engineering moved a long way in one day; the eligibility position did not.

### Weakest link

> No live YouCam evidence exists anywhere a judge can reach — not on any hosted path, not in a committed receipt, not on tape. The client (lib/youcam/client.ts: presigned-PUT header merge, flat-body runTask, poll to task_status, 429/5xx retry) is genuine, non-trivial, first-party work and it is 100% dormant at runtime for every judge without an out-of-band code. That single fact is billed five times: it caps technological_implementation at 6 against a criterion that says 'provable from the running product', caps the special_skin_vto_fusion tiebreaker at 6 because an invariant fixture read cannot be seen to determine anything, fails Stage-One gate 2 on both legs, empties depth checklist items 2/3/4, and is what the hero stat block over-claims to cover. One approved units-on run with a committed receipt would move four of those five at once.

## Per-lens scores

| Lens | technological | design | potential | quality | special | depth | overall |
|---|---|---|---|---|---|---|---|
| YouCam API Team — Technological Implementation lens (Perfect Corp reviewer) | 6 | 7.5 | 6.5 | 8 | 7.5 | 3 | 7 |
| YouCam API Team — Design lens (Perfect Corp reviewer: does it read as a finished product or a proof of concept) | 5 | 6 | 6 | 7 | 7 | 3 | 5.5 |
| YouCam API Team — Potential Impact lens (Perfect Corp reviewer) | 6 | 7 | 5 | 7 | 6 | 3 | 5.5 |
| YouCam API Team — Quality of the Idea lens (Perfect Corp reviewer) | 6 | 7 | 7 | 6 | 7 | 3 | 6.5 |
| YouCam API Team — Special category reviewer (Skin AI + Apparel VTO as one experience), Perfect Corp | 6 | 7 | 5 | 8 | 6 | 3 | 6.4 |

**YouCam API Team — Technological Implementation lens (Perfect Corp reviewer)** — The best-engineered YouCam client I have read in this track, and I could not witness it make a single call — a genuinely non-trivial integration that is 100% dormant on every path a judge can reach, topped by a hero stat block that credits Perfect Corp with four chained APIs on a page where zero were called.

**YouCam API Team — Design lens (Perfect Corp reviewer: does it read as a finished product or a proof of concept)** — The most design-literate build in the track — measured contrast tokens, clean 320px and 200% reflow, full keyboard traversal, real empty states — and its flagship proof card, live right now, labels the YouCam render "The photo" and the untouched original "YouCam render".

**YouCam API Team — Potential Impact lens (Perfect Corp reviewer)** — A genuinely well-built occasion concierge whose one defining mechanism — a countdown timed to your actual date — silently does not work for any calendar date a real person would type, and whose demo presets are curated around the two phrasings that do.

**YouCam API Team — Quality of the Idea lens (Perfect Corp reviewer)** — The best skincare-timing reasoning I have seen on our APIs — repurposing skin health scores as a scheduling signal is genuinely non-obvious — but the LLM that the whole pitch rests on never runs for me, and the rule engine that does run mis-dated a realistic sentence by three weeks on my first try.

**YouCam API Team — Special category reviewer (Skin AI + Apparel VTO as one experience), Perfect Corp** — The fusion is architecturally right and narrated on one screen in one run — skin, then colour, then a garment chosen for that colour, then the VTO render — but I watched a recording of it, not it: every face returns the identical fixture read, the masculine path drops the colour link entirely, the selfie-only path renders no garment at all, and the hero tells me four Perfect Corp APIs just fired while the ledger three screens down says nothing was called.

## Fatal flaws

1. **STAGE ONE GATE 5, BREACHED AT THE CLAIM LAYER — a false claim renders right now on the hosted instance, in the first viewport, and I confirmed both strings in the live HTML today. components/Concierge.tsx:210-228 renders unconditionally '4 / Perfect Corp APIs chained in one run' and '0–100 / skin health scores, read from your own photo'. On every path an anonymous judge reaches, zero APIs are called and the ten scores are one fixture — two different sample photos return byte-identical dark_circle_v2:59 / firmness:66 / moisture:69. This is verbatim the over-claim the team fixed in the provenance ledger and congratulates itself for in the comment at Concierge.tsx:904, surviving 700 lines above the fix with state.demo already in scope. The comment at 205-209 compounds it by claiming 'each figure here is checkable against /healthz' — /healthz reports 8, not 4.**
   - fix: Gate the hero <dl> on state.demo, exactly as the ledger already is: in demo mode name captured samples and zero units, and say the scores were read from the bundled sample photo, not from your own. Fix the 205-209 comment or make the counts actually agree with /healthz. Two-line diff, and it is the single highest score-per-hour item on the sheet.
2. **STAGE ONE GATE 2 — the required technology never fires for a judge, and the gate's own cited evidence 404s. No YouCam packet left the box during this review; live_runs_used held 0/8 across every run; /healthz self-reports youcam:'key_present_unverified', degraded:true. app/api/dev/verify/route.ts:18-20 hard-returns 404 under NODE_ENV=production and I re-confirmed HTTP 404 on the live host. No committed receipt with a genuine Perfect Corp task_id exists anywhere. The API's correctness is inferred from code reading and a '// verified against the live API (2026-07-19)' header comment.**
   - fix: One approved units-on run through skin → skin-tone → Apparel VTO → lighting, with the genuine task_ids, raw poll responses, render hashes, a same-minute /healthz snapshot and the deployed revision committed as hackathon/receipts/001-<date>/; serve it from GET /api/receipt and link each ledger row to its step. Then un-404 /api/dev/verify in production behind the existing role cookie so a judge can re-fire the schema pin without a rebuild.
3. **STAGE ONE GATE 4 — no video reaches any judge, and the cut that exists must not ship as-is. Two competing unpublished cuts (87.200 s gitignored; 122.306 s outside the repo), videoUrl='UNKNOWN', zero youtube/vimeo/loom URLs in the tree. The 87 s cut carries a banned word in the burned-in subtitle and VO at ~0:52 ('so you show up glowing') — flagged by review 001 with an explicit re-dub instruction and unchanged since, mtime Jul 27 15:33 — and at ~0:30 it preserves the pre-fix provenance ledger crediting four Perfect Corp APIs with green ticks during a fixture run. It also shows localhost and the pre-Aug-9 UI, so it misrepresents the product a judge would drive.**
   - fix: Re-cut against the hosted instance with the banned line re-dubbed, the current ledger on screen, a live-unlock hero beat and an on-camera honest-refusal self-catch; upload one canonical public cut, paste the URL into Devpost and hackathon/config.json, and delete or archive the competing cut.
4. **STAGE ONE GATE 3, THIRD LEG — the live engine is unreachable through the submission. grep 'max-gutowski' in README.md and submission/devpost.md: 0 and 0. The landing page links to /unlock zero times (confirmed on the live HTML). No judge code is published anywhere because no submission exists. devpost.md 'How to test it' offers only git clone + npm run dev, and its 'What's next' still lists the hosted live-LLM deploy as future work — it shipped 2026-08-09.**
   - fix: Hosted URL and a headline stat block at the TOP of devpost.md and README.md; judge code plus the /unlock instruction in Devpost's testing field; a visible /unlock link in the product header; delete the stale 'What's next' bullet.
5. **DESIGN, AND AN INTEGRITY LEAK IN THE SAME FAMILY — the flagship proof card labels the render and the original the wrong way round on the hosted instance. components/BeforeAfter.tsx clips `after` to inset(0 (100-pos)% 0 0), placing the render on the LEFT, while beforeLabel sits left-3 and afterLabel right-3. The hero card titled 'Rendered on a real photo' therefore shows the man in the Slate Blue Three-Piece Suit under a pill reading 'The photo' and the untouched original under 'YouCam render' — inverted at both call sites, under a caption reading 'Not an illustration, not a stock pair.' For a project whose whole moral posture is that no render is passed off as something it isn't, the provenance pills are miswired at the surface.**
   - fix: Swap the two label positions or flip the clip to inset(0 0 0 pos%), pair the skin overlay with the same person's selfie so the comparator stops reading as YouCam having changed the face, then re-capture the hero card, the gallery and the video hero beat from the corrected build.
6. **DESIGN ANCHOR FAILED — a keyboard user cannot complete the product's required first step. Both input[type=file] elements carry className="hidden", removing them from the accessibility tree; 27 tab stops through the live page never reach an upload input. 'Upload one selfie' is the pitch, and by keyboard the only way in is the two bundled sample buttons. The 9-10 design anchor names keyboard reachability explicitly.**
   - fix: Swap display:none for an sr-only clip, wire an explicit <label htmlFor>, add a real focus-visible ring on the dropzone, and re-verify by tabbing from occasion to submit with no mouse at 320px and 200% zoom.
7. **THE PRODUCT CANNOT READ A DATE — the one input its named audience is defined by. lib/concierge/occasion.ts parseDays() has grammar for 'in N weeks', 'tomorrow', 'next week' and weekday names, and nothing else: '5 September 2026', 'September 5', '2026-09-05', '05/09/2026', 'Sept 3rd' and 'August 22nd 2026' all yield no daysUntil, fall back to a canned three-week arc, and then ask the user for the date they just supplied. There is no date field in the UI. The agentic engine cannot rescue it either — prompt.ts:29 tells the model to infer the horizon from phrasing but never supplies today's date. Both engines are structurally incapable, and the two one-click sample presets inject precisely and only the two grammars the parser handles, so the demoed happy path never exposes it.**
   - fix: Add a date field beside the occasion box, compute daysUntil from it, pass today's date into the agentic prompt, render the horizon in the results header ('26 days to 5 September'), teach parseDays ISO / 'August 21' / '21 Aug' / 'the 21st' with year rollover, suppress the 'tell me the date' line whenever a date parsed, and add one regression test per format plus a 2-day case that must not front-load actives inside the taper window.
8. **VOCABULARY LAW, TWO LIVE HITS, BOTH FLAGGED YESTERDAY AND UNCHANGED — HACKATHON.md:30 still reads 'nothing simulated', using a banned word and dropping the mandated second half of the reframe so the project's own charter reads as an unqualified live-render claim; and the video carries 'glowing' at ~0:52. Judge-facing text copy is otherwise clean: zero hits for all six banned terms across README.md, submission/devpost.md and components/Concierge.tsx, and all three golden reframes are honored rather than merely avoided. There is no enforcement mechanism — no test asserts the ban, and lib/concierge/prompt.ts guards only three of the six terms, leaving the agentic path (live on gpt-5 right now) unguarded on flawless / glowing / perfect skin / simulated.**
   - fix: Fix HACKATHON.md:30 to the mandated reframe, re-dub the video line, extend prompt.ts to all six terms, and add tests/vocabulary.test.ts that fails the build on any banned term in user-facing copy.
9. **NO CLAIM-TO-PROOF MAP, AND FIVE-WAY NUMBER DRIFT DOWNSTREAM OF IT — hackathon/CLAIM_PROOF_MAP.md does not exist while HACKATHON.md standing order 2 delegates measured-claims enforcement to it. Result: devpost.md:31 says '45 Vitest tests' against a measured 89 in 10 files, under a banner reading 'Measured claims only'. The API count reads 4 / 7 / 8 across the live hero, devpost.md and /healthz, where lookVto is dead config (one grep hit, its own definition) and makeup/hairstyle slugs are the team's own 'UNVERIFIED (the demo key 401s)'. Basket $707 vs $576, Claude vs gpt-5, 'keyless' vs auth_enabled:true. config.json's own description of the test-count gap misstates it as '57 in 8 files in README/HACKATHON.md' — wrong on count and location; the drift doc has itself drifted.**
   - fix: Write CLAIM_PROOF_MAP.md as CLAIM | WHERE IT APPEARS | PROOF ARTIFACT | MEASURED ON, with every unproven row labelled 'Not yet measured' out loud; reconcile the API count to one honest set (8 declared, 7 with a calling module, 5 slug-verified, 4 that render, 0 exercised live), fix 45→89, delete lookVto, and add a test that fails when the documented count disagrees with the suite.

## Punch list, ranked by score-per-hour

| # | Pri | Owner | Item |
|---|---|---|---|
| 1 | P0 | CODE | One commit: gate the hero on demo mode, un-invert the before/after labels, link /unlock |
| 2 | P0 | CODE | Put the hosted URL, a judge code and headline stats at the top of the submission copy |
| 3 | P0 | CODE | Un-404 /api/dev/verify in production behind the existing role cookie |
| 4 | P0 | CODE | Spend units once, on the record, and commit the receipt |
| 5 | P0 | HUMAN | Re-cut and publish exactly one canonical demo video |
| 6 | P0 | HUMAN | Actually submit on Devpost, with both gating fields filled |
| 7 | P1 | CODE | Draw the fusion chain and the trust boundary — one diagram, one screen |
| 8 | P1 | CODE | Make the fusion falsifiable: a second skin fixture and per-garment renders |
| 9 | P1 | CODE | Write the claim-to-proof map, reconcile every number, and enforce both in CI |
| 10 | P1 | CODE | Teach the product to read a date |
| 11 | P1 | CODE | Make the selfie upload keyboard-reachable |
| 12 | P1 | CODE | Deploy HEAD so the revision a judge reads matches the repo they clone |
| 13 | P1 | HUMAN | Capture an annotated screenshot gallery from the corrected hosted build |
| 14 | P2 | CODE | Walk the Devpost rules line by line and record this review |
| 15 | P2 | CODE | Cut the three non-rendering studio APIs from the copy, or caption them as not shipped |
| 16 | P2 | CODE | Give the commercial half of the impact story one honest anchor |

### What each item moves

**1. One commit: gate the hero on demo mode, un-invert the before/after labels, link /unlock** (P0, CODE) — Stage One gate 5 breach + gate 3 third leg + the Design judge's fatal flaw + the Special-category judge's fatal flaw + technological_implementation credibility
**2. Put the hosted URL, a judge code and headline stats at the top of the submission copy** (P0, CODE) — Stage One gates 3 and 4 + Depth & Presentation checklist 6, 7, 12, 13 + all five judges' presentation notes
**3. Un-404 /api/dev/verify in production behind the existing role cookie** (P0, CODE) — Stage One gate 2 (first leg) + Depth checklist 13 + the Technological Implementation judge's flip-to-champion
**4. Spend units once, on the record, and commit the receipt** (P0, CODE) — Stage One gate 2 (second leg, the only thing that closes it) + technological_implementation 6→9 per the Perfect Corp lens + special_skin_vto_fusion + Depth checklist 9 and Depth items 3 and 4
**5. Re-cut and publish exactly one canonical demo video** (P0, HUMAN) — Stage One gate 4 (never-cut list) + Depth & Presentation checklist 1, 2, 3, 5, 14 — the largest single move available on the diagnostic criterion
**6. Actually submit on Devpost, with both gating fields filled** (P0, HUMAN) — Stage One gates 3 and 4 + Depth & Presentation checklist 6, 7, 13
**7. Draw the fusion chain and the trust boundary — one diagram, one screen** (P1, CODE) — special_skin_vto_fusion (TIEBREAKER — 'the judge sees that chain happen') + Depth & Presentation checklist 10
**8. Make the fusion falsifiable: a second skin fixture and per-garment renders** (P1, CODE) — special_skin_vto_fusion (TIEBREAKER — the Special-category judge's fatal flaw) + technological_implementation + quality_of_the_idea
**9. Write the claim-to-proof map, reconcile every number, and enforce both in CI** (P1, CODE) — Depth & Presentation checklist 8, 12, 14 + Depth items 6 and 9 + cross-cutting vocabulary law + technological_implementation credibility
**10. Teach the product to read a date** (P1, CODE) — potential_impact (the Impact judge's fatal flaw) + quality_of_the_idea (the Idea judge's fatal flaw) + special_skin_vto_fusion (the countdown half of the chain)
**11. Make the selfie upload keyboard-reachable** (P1, CODE) — design (the Design judge's second named fatal flaw, on the flagship control)
**12. Deploy HEAD so the revision a judge reads matches the repo they clone** (P1, CODE) — Depth & Presentation checklist 12 + Stage One gate 3 durability
**13. Capture an annotated screenshot gallery from the corrected hosted build** (P1, HUMAN) — Depth & Presentation checklist 11 + design (first impression for a skimming judge)
**14. Walk the Devpost rules line by line and record this review** (P2, CODE) — Depth items 6 and 8 + Depth & Presentation checklist 8 + closes the dangling-pointer credibility problem in config.json
**15. Cut the three non-rendering studio APIs from the copy, or caption them as not shipped** (P2, CODE) — technological_implementation (three named Perfect Corp APIs a judge taps and gets nothing) + quality_of_the_idea credibility + Depth & Presentation checklist 12
**16. Give the commercial half of the impact story one honest anchor** (P2, CODE) — potential_impact (the Impact judge's second deduction: the commercial story is asserted, not evidenced)

## Cut list

- CUT the competing 122.306 s reel at /home/sansforensics/aphrodite-demo/aphrodite-demo.mp4 the moment one canonical cut is published. Two videos with nothing marking which is canonical is worse than one.
- CUT the 6 stale screenshots at /home/sansforensics/aphrodite-demo/screenshots/ (Jul 26, outside the repo, pre-gate, pre-announcement-bar, pre-deploy) and un-mark the HACKATHON.md '**done**' row. An asset that advertises a UI that no longer exists costs more than an empty gallery.
- CUT the 'What's next' bullet in submission/devpost.md promising 'a one-click hosted deploy with a live LLM key + spend cap'. It shipped 2026-08-09. It is currently the single most expensive sentence in the packet: it tells a skimming judge the project's best asset does not exist.
- CUT '45 Vitest tests' from devpost.md:31 and the '7 YouCam APIs' framing from README.md and devpost.md. Replace with the measured 89 in 10 files and one honest surface statement (8 declared / 7 with a calling module / 5 slug-verified / 4 that render).
- CUT lookVto from lib/youcam/config.ts:17 — zero call sites, verified by grep, and it inflates the youcam_apis_wired:8 figure the hero's own comment invites judges to cross-check against.
- CUT 'nothing simulated' from HACKATHON.md:30. A banned word plus a dropped reframe, flagged by review 001 with an explicit cut instruction and unchanged 24 hours later, in the project's own charter.
- CUT the three studio try-ons from the advertised API list (or caption them 'Not shipped'). Hair colour, hairstyle and makeup render nothing on any reachable path and two of three slugs were never verified. This is the project's own cutOrder item 1 and it has been carried while never-cut items went undone — a cut-order inversion.
- CUT the 'Occasion Makeup Edit' basket row while no render backs it, and cut inStock:true across all catalogue entries in favour of a neutral field.
- DEMOTE, do not delete, the 'two engines, one event stream' headline until the agentic engine can be seen running. It is live on gpt-5 but behind a code no judge holds, so as a lead claim it is unfalsifiable; keep it as an architecture note in the diagram until rank 5's hero beat shows it firing.
- DO NOT CUT (never-cut list, all load-bearing): the public repo and MIT LICENSE; the demo video, uploaded and linked on Devpost; a hosted judge URL that stays live through judging; the zero-unit fixture fallback on every demo path; the honest demo-mode labelling. Also do not cut, though they are not on that list, the four things this panel unanimously named the best work in the repo: /healthz's three-state youcam field and its refusal to probe because probes burn units; the fail-closed ledger claim-before-run and the externally checkable spend meter; the honest degradation that returns text instead of fabricating a render; and the required-cut selector with its documented refusal to offer 'no preference' against a 9:1 catalogue. Do not cut config.json's disclosure that the gpt-5 key is shared with the Blast Radius instance, or the prior-work fence — both cut against the author's interest and both are why this packet is trusted where it is trusted.

## Correctness & vocabulary audit

- **Correctness risk:** [object Object]
- **Prior-work fence:** ok
- **Vocabulary hits:**
  - HARD HIT, still uncorrected — HACKATHON.md:30: "shoppable look board — every image a real YouCam render, nothing simulated." Uses the banned word "simulated" AND drops the mandated second half of the golden reframe ("in demo mode they are captured samples and the page says so"), so the project's own charter reads as an unqualified live-render claim. hackathon/reviews/001-2026-08-09-baseline.md:97 flagged this exact line yesterday with an explicit CUT instruction. The line is unchanged.
  - HARD HIT, verified today by frame extraction — submission/aphrodite-demo.mp4 at ~0:50 carries a burned-in subtitle and voiceover reading: "It even plans a skincare countdown timed to the day. Actives early, calming and hydration the night before, so you show up glowing." "glowing" is explicitly banned. The file mtime is Jul 27 15:33, unchanged since review 001 flagged this and prescribed a re-dub — so the correction was never made. This sits in the artifact destined for the Devpost video field, i.e. the one deliverable a grep-based vocabulary audit structurally cannot see. (frame extracted and inspected) Note the same frame also shows the pre-Aug-9 UI and "Ivory Tailored Pantsuit", so the reel misrepresents the product a judge would drive.
  - BORDERLINE, pervasive, no ruling on record — "glow" as a product noun across ~15 user-facing strings: components/Concierge.tsx:25 ("Multi-week glow arc"), :1273 ("Start a glow check-in"), :1300 ("Your glow check-in"), :1456 (a "Glow" skin-focus control), components/Companion.tsx:74 ("Track my glow"), lib/concierge/deterministic.ts:240 ("Evens tone and adds glow"), :247 ("bring back glow with vitamin C daily"), lib/concierge/types.ts:54 and lib/concierge/request-schema.ts:36 (skinGoal enum includes "glow" in the PUBLIC API), README.md:13, submission/devpost.md:23, submission/demo-video-script.md:38. The ban is on "glowing", not "glow", so this survives a literal read — but it is one inflection from a banned word across dozens of strings, and there is no ruling on record because hackathon/VOCABULARY.md does not exist; the law survives only inside hackathon/config.json:167-172.
  - NO ENFORCEMENT MECHANISM — grep over tests/ for the ban list returns nothing; there are 10 test files (catalog, cutPreference, gate, ledger, occasion, pickGarment, rate-limit, request-schema, sanitize, studio) and not one asserts banned words are absent from copy or from streamed narration. The only enforcement anywhere is lib/concierge/prompt.ts:9 and :18, which instruct the LLM never to use "flaw"/"problem"/"camouflage" — leaving flawless, glowing, perfect skin and simulated unguarded on the AGENTIC path, which is the headline differentiator and the one path whose wording is not human-authored. /healthz reports agentic_engine:"live" with gpt-5, so that path is armed on the hosted instance right now.
  - CLEAN, WITH CREDIT — all three golden reframes are honored, not merely avoided. No "hide your flaws" anywhere; instead components/Concierge.tsx:944 "Your priority areas — the lowest of 10 YouCam scored" and lib/concierge/deterministic.ts "support your priority area". No "AI-generated look"; instead the hero caption "the Slate Blue Three-Piece Suit, put on this photo by the YouCam Apparel VTO API. Not an illustration, not a stock pair." No "we simulate the result"; the ledger says "Captured from YouCam AI ... Nothing was called just now." README.md and submission/devpost.md are both clean on all six banned terms (grep: zero hits).
- **Cut-list violations:**
  - NEVER-CUT ITEM UNDELIVERED — "demo video uploaded and linked on Devpost". Neither of the two cuts is on any host, and neither has been touched since review 001 declared this fatal: submission/aphrodite-demo.mp4 (9,077,910 bytes, mtime Jul 27 15:33, 87.2 s) and ~/aphrodite-demo/aphrodite-demo.mp4 (2,924,156 bytes, mtime Jul 26 10:08, 122.3 s). Nothing marks which is canonical. hackathon/config.json:236 still reads videoUrl: "UNKNOWN — first cut exists untracked at submission/aphrodite-demo.mp4 (~90 s), not uploaded". grep for youtube|youtu.be|vimeo across README.md, submission/devpost.md and hackathon/config.json returns zero hits. .gitignore:49 promises "the reel lives on YouTube/Vimeo, not in git" — it lives on neither.
  - NEVER-CUT ITEM UNDELIVERED — "hosted judge URL that stays live through judging" is live but ABSENT from both judge-facing documents. grep for aphrodite.max-gutowski.de across README.md and submission/devpost.md: ZERO hits. The URL appears only in HACKATHON.md:96 and hackathon/config.json, neither of which a judge reads. submission/devpost.md's "How to test it" still instructs git clone + npm install + npm run dev, and its "What's next" still lists "a one-click hosted deploy with a live LLM key + spend cap" as FUTURE work — which shipped 2026-08-09. A judge reading the submission concludes no hosted demo exists while a working one answers on port 443.
  - CUT-ORDER INVERSION — cut item #1 (studio try-ons: hair colour, hairstyle, makeup) is still shipped as three code paths AND still sold in README.md:13 and submission/devpost.md:23, while rendering nothing on any reachable path. I drove all three: each returns no image and an honest empty state. So three of the seven advertised APIs are permanent empty states, and lib/youcam/config.ts:19-24 admits two of the three slugs were never even verified (401s on the demo key). The project's own cut order says these go FIRST — they should have been dropped from the copy before the never-cut items were left undone.
  - ACCESS GATE UNDISCOVERABLE — the landing page contains ZERO links to /unlock (curl -s https://aphrodite.max-gutowski.de/ | grep -c '/unlock' returns 0), and neither submission/devpost.md nor README.md names the route. /unlock itself is well built and states "8 of 8 live runs left on this instance", but a judge holding a code has no on-page affordance to reach the live engine — so the never-cut "role code unlocks the live paths" half of Stage-One gate 3 is functionally unreachable through the submission.

## Bottom line

OFFICIAL: 30/50, up +4 from the 26/50 baseline. Submission Depth & Presentation: 3.5/10 (diagnostic, not folded in), up +0.5. Stage One: FAIL — gates 2, 3 and 4 fail outright and gate 5 is breached at the claim layer; only gate 1 passes cleanly. The 30 is academic until that changes.

The panel is unanimous on the shape and unusually sympathetic. In one day this went from "HOSTED: dead, could not resolve host, there is nothing to drive as a judge" to a live instance any anonymous visitor can drive through the full fused loop in 4.4 seconds at zero units, with tracked deploy artifacts, a fail-closed health gate, an HMAC access gate and a spend ledger that held 0/8 across every run this panel drove. lib/youcam/client.ts implements the genuine four-step Perfect Corp contract — presigned-PUT header merge, flat-body runTask, poll to task_status, 429/5xx retry — and nobody writes that subtlety without having hit the real API. The honesty engineering is the best thing here and it is not decorative: /healthz refuses a green dashboard, the ledger says "Nothing was called just now", the refine step declines to render rather than inventing an image, the studio taps return honest empty states, and the README volunteers a mis-gendering bug it shipped and fixed. Two of the three configured integrity risks were attacked directly this cycle and held.

And a judge gets almost none of it. The hosted URL appears in zero judge-facing documents (grep: 0 in README.md, 0 in devpost.md). The landing page links to /unlock zero times. No video is published anywhere. /api/dev/verify — the kill gate's own cited judging-time evidence — 404s in production. Six of eight battle-rhythm docs are missing, including the CLAIM_PROOF_MAP.md whose absence is exactly why "45 Vitest tests" survives against a measured 89. There is no diagram of any kind for a project whose entire differentiator is architectural, and no CI, so all four green gates are a human habit with no external anchor.

The single weakest link: no live YouCam evidence exists anywhere a judge can reach — not on a hosted path, not in a committed receipt, not on tape. That one fact is billed five times, and one approved units-on run with a committed receipt pays four of those bills at once.

Three findings go beyond "unfinished" into things to fix on principle. The hero stat block ships verbatim the over-claim the team fixed in the ledger and congratulates itself for in a comment 700 lines below — I confirmed both false strings in the live HTML today, and state.demo is already in scope. The before/after comparator's provenance pills are inverted on the hosted hero, so the flagship proof card labels the YouCam render "The photo" and the untouched original "YouCam render", under a caption reading "Not an illustration, not a stock pair." And the video that is one paste from publication carries a banned word at ~0:52 plus the pre-fix four-green-tick ledger at ~0:30, both flagged yesterday and both unchanged. These are seam problems in a project whose instincts are already right, not culture problems — which is why they are cheap and why leaving them is inexcusable.

Order of operations is not negotiable. Rank 1 first: one commit gating the hero on demo mode, un-inverting the comparator labels, and linking /unlock — minutes of work that closes a gate-5 breach and two judges' named fatal flaws. Then paste the hosted URL and headline stats into the submission copy, un-404 the verify route, get approval and spend units once with a committed receipt, re-cut the video against the hosted instance with the live hero beat and the on-camera self-catch, and submit. After that, favour the tiebreaker: the trust-boundary diagram and a second skin fixture that makes two different faces produce two different garments are what convert a chain the panel could only read into one a judge can watch. And fix the date parser — a product whose entire premise is the plan for a specific day still cannot read a specific day.

Cut before you add. The stale screenshots, the competing reel, the "45 tests" number, the dead lookVto, the three non-rendering studio APIs and the "What's next" bullet that denies the hosted deploy exists are all costing credibility they cannot repay. Seven days to the deadline, five to the self-set target, and every remaining Stage-One blocker is an upload, a paste or a five-line diff.
